.terraform/
#modules/jenkins-server/.terraform/
#modules/maven-sonarqube-server/.terraform/
#modules/s3-dynamodb/.terraform/
#modules/terraform-node/.terraform/
demo-18b-MODULE-SETUP/dev/.terraform/providers/registry.terraform.io/hashicorp/aws/5.88.0/darwin_amd64/terraform-provider-aws_v5.88.0_x5

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*
*.tfplan

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
#Ignore visual studio workspace
*.code-workspace
